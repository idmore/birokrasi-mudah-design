mixin card-subtitle(title, subtitle)
    .subtitle.align-center 
        h3.title #{title}
        p #{subtitle}

mixin card-subtitle-loading(text)
    .subtitle.align-center 
        h3.title #{text}
